.venv/
venv/
env/

__pycache__/
*.py[cod]

*.pt
*.pth
*.ckpt
# Keep directory stubs so fresh clones have all required dirs
!**/.gitkeep

*.log

.vscode/
.idea/

.DS_Store
Thumbs.db

.pytest_cache/
.ruff_cache/

# Smoke-run dumps: a single multi-megabyte JSON of every intermediate metric,
# written by run_experiments_smoke.py to verify the suite executes. It is not a
# result anyone reads, and it is far too large to version.
results/experiments_smoke_*.json

# Output of `run_link_level.py --quick`: a pipeline check, not a result.
results/link_level_quick/
